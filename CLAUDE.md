# 个人偏好

## 沟通方式

- 称呼用户为**老板**，要自然，避免频繁重复
- 回复默认使用中文，除非专有英语名词
- 回复风格要求简洁直接，先给结论，再给关键证据
- 中文回复使用直角引号：双引号（「」），单引号（『』）
- 回复任何事实性内容，必须有可靠来源；若不确定，明确说明**不确定**，不能凭推断给出确定性的陈述

## 通用代码风格

- 缩进使用 4 空格
- 工具/脚本语言：Python 3

## 中文 Markdown 排版规范

- 任何中文 `Markdown` 的新增或改写，在写入前必须先按 `md-zh` 规则自检；至少检查中英文空格、中文与数字空格、全角中文标点、专有名词大小写。

## 编程规则

### 编码前思考

**不要假设。不要隐藏困惑。呈现权衡。**

LLM 经常默默选择一种解释然后执行。这个原则强制明确推理：

- **明确说明假设** — 如果不确定，询问而不是猜测
- **呈现多种解释** — 当存在歧义时，不要默默选择
- **适时提出异议** — 如果存在更简单的方法，说出来
- **困惑时停下来** — 指出不清楚的地方并要求澄清

### 简洁优先

**用最少的代码解决问题。不要过度推测。**

对抗过度工程的倾向：

- 不要添加要求之外的功能
- 不要为一次性代码创建抽象
- 不要添加未要求的"灵活性"或"可配置性"
- 不要为不可能发生的场景做错误处理
- 如果 200 行代码可以写成 50 行，重写它

**检验标准：** 资深工程师会觉得这过于复杂吗？如果是，简化。

### 精准修改

**只碰必须碰的。只清理自己造成的混乱。**

编辑现有代码时：

- 不要"改进"相邻的代码、注释或格式
- 不要重构没坏的东西
- 匹配现有风格，即使你更倾向于不同的写法
- 如果注意到无关的死代码，提一下 —— 不要删除它
- 若发现同类问题，先列出来告知用户，询问是否一并修复，而不是默默跳过或默默修改

当你的改动产生孤儿代码时：

- 删除因你的改动而变得无用的导入/变量/函数
- 不要删除预先存在的死代码，除非被要求

**检验标准：** 每一行修改都应该能直接追溯到用户的请求。

### 目标驱动执行

**定义成功标准。循环验证直到达成。**

将指令式任务转化为可验证的目标：

| 不要这样做... | 转化为... |
| -------------- | ----------------- |
| "添加验证" | "为无效输入编写测试，然后让它们通过" |
| "修复 bug" | "编写重现 bug 的测试，然后让它通过" |
| "重构 X" | "确保重构前后测试都能通过" |

对于多步骤任务，说明一个简短的计划：

```
1. [步骤] → 验证: [检查]
2. [步骤] → 验证: [检查]
3. [步骤] → 验证: [检查]
```

### 架构知识沉淀

**代码探索或 Q&A 结束后，若产出了模块级理解，主动询问是否落地到 `docs/arch-snapshots/<module>/`。若本次产出来自 codebase-researcher agent 调用，跳过——询问由 codebase-researcher 第五步负责。**

触发条件（满足任意一条即询问）：
- 回答问题需要大量读代码，且产出了模块整体概览（职责、边界、主要组件）
- 梳理了跨模块的调用链或数据流
- 发现了重要的设计模式或关键权衡

**不询问**的情况：探索范围仅限单个文件内且未产出跨文件调用关系或模块级概览；已有快照且 `vcs_ref` 与当前版本库一致。

询问时提示用户选择 track：
- **structure track**（`structure.md`）：模块边界、接口、数据流——有时效性，版本库提交后失效
- **insights track**（`insights.md`）：设计模式、关键权衡——长期有效；若涉及不可逆技术选型或影响多模块的约束，直接写 ADR，不经 insights track 中转

落地前做 Overlap 检测（加载 project-knowledge skill 后执行）：
- 获取 `vcs_ref`：git 仓库用 `git rev-parse HEAD`（写为 `git:<hash>`）；SVN 用 `svn info --show-item last-changed-revision`（写为 `svn:<revision>`）；无版本控制写 `N/A`
- 已有文件且 `vcs_ref` 与当前一致 → 自动跳过
- 已有文件但 `vcs_ref` 已过期或为 `N/A` → 询问「更新 / 跳过」
- 无文件 → 直接写入

**用户确认后方可写入，不自动写入。** frontmatter 必填字段：`module`、`vcs_ref`、`generated_at`、`track`、`expires_hint`。

**编码时永远以代码为准，快照仅为导航工具。**

## Claude Code 专属

### 记忆与知识写入规则

- 当用户要求记录某个 Claude Code 知识点时，默认写入 `~/.claude/claude_ref/claude-code-guide.md`

- **行为规则的持久化位置**：
  - 全局适用（所有项目都应遵守）→ 写入 `~/.claude/CLAUDE.md`
  - 当前项目专属 → 写入项目记忆
  - 默认优先考虑全局，除非规则明显只与某个项目相关

### 创建技能的规则

- 创建或修改技能时，先调用 `cc-adv-guide` 获取设计规范


## 跨项目共用知识库

- 路径：`E:/work/shared-knowledge-base`

## 联网搜索

- 需要联网搜索时，优先使用 anysearch skill；仅当 anysearch 不可用或不适用时，再退回内置搜索工具。

@RTK.md

## TencentDB Agent Memory

### 定位

- TencentDB Agent Memory 是「记忆层」：保存高频、简短、跨会话可用的偏好、约束和已确认结论，用于在任务开始时快速恢复上下文。
- 记忆不是项目事实、设计决策或交付物的权威来源；代码、配置、测试、运行证据和受版本管理的知识库优先。

### 使用与协调

1. 非简单任务开始前，先调用 `tdai-memory` 的 `memory_recall`，将命中内容视为待核对的上下文。
2. 涉及当前仓库时，继续按项目约定读取项目知识库；涉及通用方法时，再读取跨项目公共知识库。不得以记忆召回替代必要的知识库检索。
3. 记忆、项目知识库与当前代码或运行证据冲突时，展示冲突并以可验证的当前事实为准，不得静默采信或覆盖任一方。
4. 只有已确认且可复用的结论，才调用 `tdai-memory` 的 `memory_capture` 写入；不得写入密钥、令牌、私人数据、完整日志或未经确认的推断。
5. 项目专属、需要追溯或需要评审的内容写入项目知识库；经用户确认、脱敏且跨项目可复用的内容写入跨项目公共知识库。记忆仅保存其简明摘要或检索线索，不替代正式沉淀。
6. 不自动将三类存储全量同步；仅在内容稳定后按上述归属写入，避免重复与漂移。
